#include <stdio.h>

int main() {

    long long int A, B, multiplication;
    scanf("%lld %lld", &A, &B);
    
    multiplication = A * B;
    
    printf("%lld", multiplication); 
    
    return 0;
}